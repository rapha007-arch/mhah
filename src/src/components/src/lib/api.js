// API helper (kely fotsiny)
export const api = {
  get: async (url) => {
    console.log('API GET:', url);
    return { data: null };
  },
  post: async (url, data) => {
    console.log('API POST:', url, data);
    return { data: null };
  }
};
